{
    "name": "@swader/x402-cli",
    "version": "0.1.0",
    "author": "https://github.com/swader",
    "description": "CLI tester for X402 endpoints",
    "type": "module",
    "bin": {
        "x402": "dist/cli.js"
    },
    "scripts": {
        "build": "mkdir -p dist && bun build src/cli.ts --outdir dist --target bun --sourcemap && chmod +x dist/cli.js",
        "prepublishOnly": "bun install && bun run build"
    },
    "dependencies": {
        "dotenv": "^17.2.3",
        "viem": "^2.38.6",
        "x402-fetch": "^0.7.0"
    },
    "devDependencies": {
        "bun-types": "^1.3.1",
        "typescript": "^5.9.3"
    }
}